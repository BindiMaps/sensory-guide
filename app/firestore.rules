rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder rules - will be expanded in Story 2.5
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
